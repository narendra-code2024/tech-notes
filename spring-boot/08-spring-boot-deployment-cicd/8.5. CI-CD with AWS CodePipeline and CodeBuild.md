> **Modern alternatives to consider:** **GitHub Actions** is the dominant CI for Spring Boot projects in 2026 — most teams use GitHub Actions + ECR + ECS Fargate as the canonical pipeline. AWS CodePipeline + CodeBuild remain valid for AWS-native shops with strict compliance requirements (everything in AWS), but should not be presented as the only option.
>
> When fleshed out, this file should also cover:
> - **Secrets** in CI: AWS Secrets Manager / SSM Parameter Store, never env vars baked into pipeline config
> - **Reproducible artifacts**: build from tagged commits, no SNAPSHOT dependencies in prod artifacts
> - **OIDC federation**: GitHub Actions → AWS without long-lived access keys

> **TODO**: Content to be added.
>
> Will cover: setting up AWS CodePipeline with GitHub source, CodeBuild for Maven build + test, `buildspec.yml` structure, artifact passing to [8.4 Deploy on Elastic Beanstalk](<8.4. Deploy on Elastic Beanstalk.md>), IAM role permissions, approval stages, and build notifications.

