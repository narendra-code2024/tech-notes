> **TODO**: Content to be added.
>
> Will cover: provisioning AWS RDS (engine choice, instance size, VPC/subnet, security group rules, parameter group, connection string, credentials via AWS Secrets Manager), wiring Spring Boot `application-prod.properties` to the RDS endpoint, and migration workflow with [8.2 Database Migration with Flyway](<8.2. Database Migration with Flyway.md>) against the production DB.

