## Why API Gateway?

> Spring Cloud Gateway is the modern choice. The older **Zuul** (Zuul 1, Netflix) is in maintenance mode — don't start a new project with it.



Without a gateway, clients must know the IP and port of every microservice — and those change as services scale or restart.

```
Without API Gateway:

  Client ──→ Order Service (10.1.2.3:8081)
  Client ──→ Inventory Service (10.4.5.6:8082)
  Client ──→ Payment Service (10.7.8.9:8083)
  (client must know every address)


With API Gateway:

  Client ──→ API Gateway (single address, port 8080)
                  │
                  ├──→ Order Service
                  ├──→ Inventory Service
                  └──→ Payment Service
  (client knows only one address)
```

The API Gateway is the **single entry point** for all clients (web, mobile, etc.). It routes requests to the correct microservice behind the scenes.

---

## What Else Can API Gateway Do?

|Capability|Example|
|---|---|
|**Routing**|`/api/v1/orders/**` goes to Order Service|
|**Load balancing**|5 instances of Order Service → distribute traffic evenly|
|**Security**|Verify JWT token before forwarding request|
|**Filters**|Add headers, strip URL prefixes, redirect requests|
|**Observability**|Logging, tracing, metrics aggregation|

---

## Spring Cloud Gateway — Three Building Blocks

```
Request arrives at API Gateway
       │
       ▼
┌─────────────────────────────┐
│  Route (where to send)      │
│    │                        │
│    ├── Predicate (if match) │
│    │                        │
│    └── Filters (modify)     │
└─────────────────────────────┘
       │
       ▼
Downstream Microservice
```

|Block|What It Does|Example|
|---|---|---|
|**Route**|The destination — which service to send the request to|Order Service, Inventory Service|
|**Predicate**|The condition — only route if this matches|Path matches `/orders/**`, method is GET|
|**Filter**|Modify request/response before or after routing|Add header, strip prefix, redirect|

---

## Setup

### Step 1: Create API Gateway Service

From [start.spring.io](https://start.spring.io/):

- Artifact: `api-gateway`
- Dependencies: **Gateway** (`spring-cloud-starter-gateway`), **Eureka Discovery Client**

> Remove `-mvc` from the dependency if Spring Initializr adds `spring-cloud-starter-gateway-mvc`. API Gateway uses reactive (Netty), not MVC.

> API Gateway is also a **Eureka Client**: it uses Eureka to discover where each service lives. No hardcoded service addresses.

### Step 2: Configure Routes

```yaml
spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        # Order Service route
        - id: order-service
          uri: lb://ORDER-SERVICE
          predicates:
            - Path=/api/v1/orders/**
          filters:
            - StripPrefix=2

        # Inventory Service route
        - id: inventory-service
          uri: lb://INVENTORY-SERVICE
          predicates:
            - Path=/api/v1/inventory/**
          filters:
            - StripPrefix=2

server:
  port: 8080

# Eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
```

---

## Understanding the Config

### Route

```yaml
- id: order-service              # unique name for this route
  uri: lb://ORDER-SERVICE        # destination service
```

`lb://ORDER-SERVICE` means:

- `lb://` — use **load balancer** (if multiple instances, distribute traffic)
- `ORDER-SERVICE` — the `spring.application.name` registered in Eureka

> Gateway asks Eureka: "Where is ORDER-SERVICE?" → gets IP:port → routes there.

### Predicate

```yaml
predicates:
  - Path=/api/v1/orders/**       # route only if URL matches this pattern
```

Common predicates:

|Predicate|What It Matches|
|---|---|
|`Path=/api/v1/orders/**`|URL path matches the pattern|
|`Method=GET`|Only GET requests|
|`Method=GET,POST`|Only GET or POST requests|
|`Header=User-Agent, Mozilla/*`|Request has this header value|

### Filter

```yaml
filters:
  - StripPrefix=2                # remove first 2 path segments
```

What `StripPrefix=2` does:

```
Client sends:    /api/v1/orders/core/helloOrders
StripPrefix=2:   removes /api/v1
Service receives: /orders/core/helloOrders
```

> The `/api/v1` prefix is for the gateway's routing. The actual service doesn't know about it — StripPrefix removes it before forwarding.

---

## Common Built-in Filters

|Filter|What It Does|Example|
|---|---|---|
|`StripPrefix=N`|Remove first N path segments|`StripPrefix=2` removes `/api/v1`|
|`AddRequestHeader=Key, Value`|Add a header to the request sent to service|`AddRequestHeader=X-Custom-Header, Anuj`|
|`AddResponseHeader=Key, Value`|Add a header to the response sent to client|`AddResponseHeader=X-Response-Id, abcd`|
|`RedirectTo=status, URL`|Redirect the request|`RedirectTo=302, https://example.com`|
|`RemoveRequestHeader=Name`|Remove a header from the request|`RemoveRequestHeader=Cookie`|

### AddRequestHeader Example

```yaml
- id: inventory-service
  uri: lb://INVENTORY-SERVICE
  predicates:
    - Path=/api/v1/inventory/**
  filters:
    - StripPrefix=2
    - AddRequestHeader=X-Custom-Header, Anuj
```

The downstream service can read this header:

```java
@GetMapping("/fetchOrders")
public String fetchOrders(HttpServletRequest request) {
    String customHeader = request.getHeader("X-Custom-Header");
    log.info("Header value: {}", customHeader); // "Anuj"
    // ...
}
```

> Useful for passing user ID, token, or other info from gateway to services after authentication.

### RedirectTo Example

```yaml
- id: no-service
  uri: lb://NO-SERVICE
  predicates:
    - Path=/api/v1/no-service/**
  filters:
    - AddRequestHeader=X-Custom-Header, Anuj
    - RedirectTo=302, https://codingshuttle.com
```

> Any request to `/api/v1/no-service/**` gets redirected to codingshuttle.com.

---

## How Request Flows Through Gateway

```
Client: GET http://localhost:8080/api/v1/orders/core/helloOrders
       │
       ▼
API Gateway (port 8080)
       │
       ▼
Check predicates: Path=/api/v1/orders/** → matches order-service route
       │
       ▼
Apply filters: StripPrefix=2 → /api/v1 removed → /orders/core/helloOrders
       │
       ▼
Resolve URI: lb://ORDER-SERVICE → ask Eureka → 10.1.2.3:8081
       │
       ▼
Forward to: http://10.1.2.3:8081/orders/core/helloOrders
       │
       ▼
Order Service responds → Gateway returns response to client
```

---

## Startup Order

Start services in this order:

```
1. Discovery Service (Eureka Server) — must be up first
2. Order Service, Inventory Service (Eureka Clients) — register themselves
3. API Gateway — discovers services through Eureka
```

> If gateway starts before services register, you may get "No server available" errors. Wait ~30 seconds for Eureka to sync, then it works.

---

## Production Considerations

- **Auth at the gateway**: validate JWT / OAuth2 tokens once at the gateway and forward the verified identity to downstream services in a header. Avoids every service repeating auth logic.
- **Rate limiting**: Spring Cloud Gateway has a built-in `RequestRateLimiter` filter (token-bucket via Redis). Apply per-route or per-user to protect downstreams from spikes.
- **Circuit-breaker filter**: wrap routes with `CircuitBreaker` filter so a failing downstream returns a fallback response instead of long timeouts (see [12.6 Circuit Breaker, Retry and Rate Limiter with Resilience4J](<12.6. Circuit Breaker, Retry and Rate Limiter with Resilience4J.md>)).
- **Run multiple gateway replicas**: the gateway is the single entry point, so it's also the single point of failure. Always run 2+ replicas behind an external load balancer (cloud LB, k8s `Service` of type `LoadBalancer`).
- **TLS termination**: terminate TLS at the gateway (or one hop in front, e.g., the cloud LB / ingress). Internal hops can use mTLS or stay inside a private network.
- **Observability**: enable access logging, metrics (Micrometer + Prometheus), and propagate tracing headers (B3 / W3C Trace Context). The gateway is the natural place to inject correlation IDs that flow through every downstream call.
- **Don't put business logic here**: the gateway is for routing, auth, rate limiting, observability, and traffic shaping. Anything domain-specific belongs in the services.

> **Production warning:** Spring Cloud Gateway is reactive (Netty). Don't add servlet/blocking dependencies to the same service — they conflict and you'll get startup or runtime issues. Keep the gateway pod lean.

---

## Note on `server.servlet.context-path`

Some projects set `server.servlet.context-path: /orders` on a service so every endpoint gets that prefix automatically — `@RequestMapping("/core")` becomes `/orders/core/...`. This is **not mandatory**: putting `/orders` directly in `@RequestMapping` works the same. The gateway routes based on the predicate pattern, not the context-path:

```
With context-path:       context-path=/orders + @RequestMapping("/core")
Without context-path:    @RequestMapping("/orders/core")
→ Both produce: /orders/core/...
```

---

## What We Built

```
ecommerce/
  ├── discovery-service/    (port 8761 — Eureka Server)
  ├── api-gateway/          (port 8080 — single entry point)
  ├── order-service/        (port 8081 — Eureka Client)
  └── inventory-service/    (port 8082 — Eureka Client)
```

> Next: Instead of using `DiscoveryClient` + `RestClient` for service-to-service calls, we'll use **OpenFeign** ([12.5 Service-to-Service Communication with OpenFeign](<12.5. Service-to-Service Communication with OpenFeign.md>)) which is much simpler.

---

## Q&A

1. **Why an API Gateway in front of microservices?** A single entry point so clients don't need to know each service's address. It also centralizes cross-cutting concerns: auth, rate limiting, logging, CORS, request/response transformation.
2. **What are Routes, Predicates, and Filters in Spring Cloud Gateway?** Route = the destination (which service). Predicate = the condition that must match (path, method, header). Filter = transformations applied before forwarding (e.g., `StripPrefix`) or after (e.g., `AddResponseHeader`).
3. **What does `lb://ORDER-SERVICE` mean?** `lb://` tells Gateway to use the Spring Cloud LoadBalancer to resolve the target. `ORDER-SERVICE` is the `spring.application.name` registered in Eureka — Gateway looks up instances and load-balances across them.
4. **Why is Spring Cloud Gateway reactive (Netty), not Servlet?** It handles many in-flight requests concurrently with low resource use. The MVC variant exists but the reactive Netty stack is the standard choice for a gateway under load.
5. **Difference between API Gateway load balancing and OpenFeign load balancing?** Gateway balances client → service traffic (north-south). OpenFeign + Spring Cloud LoadBalancer balances service → service traffic (east-west). Both use Eureka under the hood.
6. **Why not put business logic in the gateway?** It's the entry point for the whole system — keeping it focused on routing/auth/rate-limiting means deploys are safe and the gateway stays fast. Domain logic in the gateway turns it into a distributed monolith with weird ownership.

---

## Quick Reference

| Concept | Remember |
|---|---|
| Gateway role | Single entry point — routing, auth, rate-limit, observability |
| Route | Destination service (`uri: lb://NAME`) |
| Predicate | The match condition (Path, Method, Header, ...) |
| Filter | Modify request/response (`StripPrefix`, `AddRequestHeader`, ...) |
| `lb://NAME` | Use Spring Cloud LoadBalancer + Eureka to resolve target |
| Stack | Reactive Netty — keep `-mvc` variant out |
| Replicas | Run 2+ instances behind an external LB; gateway is also a SPOF |
